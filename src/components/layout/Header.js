import MainNavigation from "./MainNavigation";
import ScrollToTop from "./ScrollToTop";

const Header = () => {
  return (
    <header>
      <MainNavigation />
      <ScrollToTop />
    </header>
  );
};

export default Header;
